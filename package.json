{
  "name": "interview-scheduler-monolith",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build-client": "cd client && npm install && npm run build",
    "bootstrap": "npm install && cd client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "path": "^0.12.7",
    "uuid": "^11.1.0"
  }
}
